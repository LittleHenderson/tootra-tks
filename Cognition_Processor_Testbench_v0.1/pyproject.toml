[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cognition-processor-testbench"
version = "0.1.0"
description = "Deterministic three-layer CCDL/CAS cognition processor testbench"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["cptb"]
